//Load common code that includes config, then load the app logic for this page.
requirejs(['./requirejsCommon'], function (common) {
    console.log('I am in the file');
    // requirejs(['app/main1']);
});
